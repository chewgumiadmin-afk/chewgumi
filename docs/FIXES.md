# 고칠 것 — 지금 상태

`tools/fixes.py --report` 가 만든 것입니다. **손으로 고치지 마세요** —
고칠 것은 이슈 본문의 `fix` 칸입니다 (형식은 `docs/FIX-FORMAT.md`).

```
저장소       chewgumiadmin-afk/chewgumi
바로 반영    0 건
사람 대기    1 건
반영됨       10 건
보류         0 건
```

## 바로 반영해도 되는 것

`막는것: 없음` 이고 아직 `대기` 인 것입니다. **사람 확인 없이 고쳐도 됩니다.**

없습니다.

## 사람이 정해야 넘어가는 것

| id | 이슈 | 무엇 | 막는 것 |
|---|---|---|---|
| `CG-136-1` | #136 | 가상계좌 은행 목록·입금기한을 KICC 기본값에 맡겨, 화면의 「3일」 안내와 어긋날 수 있습니다 | **bankList 은행 코드값 · expiryDate·expiryTime 자리수 — KICC 개발가이드 (이 환경에서 접속 불가). 대표님이 형식을 알려 주시면 넣습니다** |

## 이미 반영된 것

| id | 이슈 | 무엇 | 커밋 |
|---|---|---|---|
| `CG-158-1` | #158 | cg_sb 를 직접 읽는 세 자리가 만료를 보지 않아, 만료된 토큰으로 요청이 나갑니다 | `3d589fe` |
| `CG-158-2` | #158 | 검사가 .html 만 봐서 assets/*.js 의 cg_sb 직접 읽기를 못 잡았습니다 | `3d589fe` |
| `CG-157-1` | #157 | 상품 사진이 JPEG 1,263KB | `3d589fe` |
| `CG-156-1` | #156 | Claude 호출에 tools 가 없어, 규정과 현황을 다 알면서도 답이 늘 글로만 나옵니다 | `a4b13c8` |
| `CG-156-2` | #156 | 같은 모양의 질문이 올 때마다 Claude 를 다시 부릅니다 | `a4b13c8` |
| `CG-111-1` | #111 | 테스트 계정의 메일과 비밀번호가 파일에 그대로 적혀 있습니다 | `b33216d` |
| `CG-111-2` | #111 | 코드에서 빼도 저장소 기록에는 남습니다. 값을 바꾸지 않으면 그대로 쓸 수 있는 열쇠입니다 | `운영에서 처리 (2026-09-25 대표님 확인)` |
| `CG-111-3` | #111 | 관리자 계정 세 개의 비밀번호가 문서에 평문으로 적혀 있습니다 | `b33216d` |
| `CG-111-4` | #111 | 문서에서 지워도 저장소 기록에는 남습니다. 값을 바꾸기 전까지는 그대로 쓸 수 있는 열쇠입니다 | `운영에서 처리 (2026-09-25 대표님 확인)` |
| `CG-111-5` | #111 | CG-111-3 과 같은 문제가 문서 세 개에 더 있습니다. 계정 다섯 개 분량입니다 | `b33216d` |

---

반영한 뒤에는 **이슈의 `fix` 칸에서 `상태` 를 `반영됨 · <커밋>` 으로 바꾸고**,
이 파일을 `--report` 로 다시 만들어 주세요. 그래야 다음에 또 고치지 않습니다.
