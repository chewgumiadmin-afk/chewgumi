# 이 저장소에서 일할 때

츄구미 자사몰 — **아직 오픈 전입니다.** 손님 화면·결제·로그인이 걸린 곳은
한 번 잘못 밀면 장사가 멈춥니다.

---

## 1. 조사하는 쪽과 고치는 쪽이 다릅니다 ★

```
조사   예약 리서치  →  GitHub 이슈에 「고칠 것」을 적는다.  코드는 안 고친다.
반영   Claude Code  →  그 이슈를 읽고 실제로 고친다.
```

사이에 사람이 없습니다. 그래서 형식이 정해져 있습니다.

**반영하러 왔다면 먼저 이것부터 보세요.**

```
docs/FIXES.md        지금 무엇이 바로 반영 가능한지 (사람이 읽는 표)
docs/FIX-FORMAT.md   「고칠 것」을 적는 형식
tools/fixes.py       이슈에서 직접 읽어 내는 도구
```

`docs/FIXES.md` 의 **「바로 반영해도 되는 것」** 절에 있는 것만 확인 없이 고칩니다.
**「사람이 정해야 넘어가는 것」은 손대지 마세요** — 막는 것이 무엇인지 그 표에 적혀 있습니다.

**고치고 나면 두 가지를 하세요.**

1. 그 이슈의 `fix` 칸에서 `상태:` 를 `반영됨 · <커밋해시>` 로 바꿉니다
2. `python3 tools/fixes.py --report` 로 `docs/FIXES.md` 를 다시 만들어 함께 커밋합니다

이걸 안 하면 다음 번에 같은 것을 또 고칩니다.

**조사하러 왔다면** — 코드를 고치지 말고 `docs/FIX-FORMAT.md` 의 형식으로 이슈에만
적으세요. 사람이 정해야 할 것이 하나라도 있으면 `막는것` 을 반드시 채웁니다.
비워 두면 확인 없이 고쳐집니다.

`scripts/uxcheck.py` 는 **다른 체계**입니다. 기계가 규칙(R1~R15)으로 잡아 이슈를 직접
올리고, 지문으로 중복을 막습니다. 이슈 본문의 `<!-- uxcheck:fp=… -->` 는 지우지 마세요.

## 2. 확인 없이 바꾸지 않는 것

```
· 손님 화면의 문구·디자인          이슈 #108 에서 정한 작업 원칙 (R16)
· 결제 길목                        checkout.html · pay 함수 · 이지페이 전문
· 로그인 길목                      assets/tok.js · assets/guard.js · Supabase 인증 설정
· 주문·회원 데이터의 저장 형식
· 돈이 나가거나 손님에게 나가는 것  환불 실행 · 문자·메일 발송 · 과금
```

R16 을 `docs/UX-RULES.md` 에서 찾지 마세요. **그 문서는 R15 에서 끝납니다.**
R16 은 이슈 #108 에 있습니다.

## 3. 저장소에 없는 것 — 엣지 함수

화면이 부르는 Supabase 엣지 함수가 **서른 개쯤 되는데, 소스가 저장소에 없습니다.**

```
supabase/functions/card-todos   저장소에 있는 유일한 함수
pay · refund · order · admin-chat · ship-mail · notify · …
                                Supabase 에 배포된 것만 있습니다
```

그래서 **여기서 고칠 수 없습니다.** 엣지 함수를 고쳐야 하는 항목은 `막는것` 에
「저장소에 소스가 없습니다」라고 적고 넘기세요. 화면 쪽에서 우회하지 마세요 —
같은 일이 두 군데로 갈라집니다.

## 4. 로그인은 한 곳에서 읽습니다

`assets/tok.js` 가 토큰을 한 곳으로 모읍니다. **모든 화면이 이것을 먼저 싣습니다.**

```js
cgTok()      만료됐으면 '' 를 돌려줍니다          ← 이것을 쓰세요
JSON.parse(localStorage.getItem('cg_sb')).t      ← 만료를 안 봅니다
```

직접 읽으면 만료된 토큰으로 요청이 나가고, 서버는 401 로 되돌려 주며, 화면에는
이유 없이 「불러오지 못했습니다」가 뜹니다. 손님이 보면 사이트가 고장난 것으로 보입니다.
22개 화면은 이미 `cgTok()` 으로 바꿨습니다. `checkout.html` 세 자리가 남아 있습니다 (#158).

## 5. 확인

```
python3 scripts/uxcheck.py .            UX-RULES R1~R15 정적 검사
python3 scripts/uxcheck.py . --github   새 지문만 이슈로 등록 (함부로 돌리지 마세요)
python3 tools/img-check.py              상품 이미지 짝맞춤 (계열이 섞였는지)
```

`tests/flow-test.js` 는 **운영 Supabase 에 실제로 주문을 넣습니다.** 주문 →
입금확인 → 발송 → 취소를 끝까지 태우는 것이라, 확인용으로 가볍게 돌리지 마세요.

빌드가 없습니다. 정적 파일 묶음이고 Netlify 후처리도 꺼 두었습니다(`netlify.toml`) —
실제 배포와 미리보기의 주소가 달라지면 QA 결과를 믿을 수 없기 때문입니다.

색 대비를 바꿨으면 WCAG 상대휘도로 계산해 수치를 남기세요. 눈대중 금지.

## 6. 비밀값

열쇠·연결 문자열을 이슈·커밋·카톡에 넣지 마세요. 어디에 있는지만 적습니다.
대화에 노출된 열쇠 정리는 #111 입니다 — **오픈 전에 끝나야 합니다.**

손님 정보(주문·연락처·주소)를 이슈에 옮겨 적지 마세요. 필요하면 개수만 씁니다.
